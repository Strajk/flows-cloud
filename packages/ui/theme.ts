import { defineTokens } from "@pandacss/dev";

export const theme = {
  tokens: defineTokens({
    colors: {
      primary: { value: "#0FEE0F" },
      secondary: { value: "#EE0F0F" },
    },
  }),
};
